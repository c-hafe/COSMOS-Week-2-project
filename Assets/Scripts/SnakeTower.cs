using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SnakeTower : MonoBehaviour
{
    
   /* // Start is called before the first frame update
    
    //member variables

    public GameObject spit;
    public GameObject muzzle;
    public Vector3 position = transform.position; 
    public int viewRange = 20;
    public int attackRange = 15;
    public float fireRate = 2f;
    public float ballSpeed = 1000f;


    public enum SnakeState
    {
        None, FindTarget, Attack
    }

    public SnakeState state = SnakeState.None;
    public bool enemyInRange;
    public GameObject target;
    public bool enemyInAttackRange;

    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void enemyInAttackRange()
    {
        target = FindClosestTarget("Monkey", attackRange);

        return target != null;

    }

    void CheckGameState()
    {
        lastFire += Time.deltaTime;

        enemyInAttackRange = enemyInAttackRange();

        if(enemyInAttackRange)
        {
            state = SnakeState.Attack;
        }


    }
   */

}
